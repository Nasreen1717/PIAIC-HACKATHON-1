[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    ros2: ROS 2 specific tests
    integration: Integration tests requiring full setup
    slow: Slow running tests
